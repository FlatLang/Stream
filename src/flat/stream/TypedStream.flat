package flat/stream

import flat/datastruct/HashMap

class TypedStream<Type> {
  let Stream stream = Stream()

  public async emit(String eventType, Type data) => this {
    stream.emit(eventType, data)
  }

  public on(String eventType, async action(Type)) => this {
    stream.on(eventType, action)
  }
}
